'use client';
import {ReactNode,useEffect,useRef} from 'react';
import {X,Plus,ArrowUpRight} from 'lucide-react';
export function Modal({title,children,onClose,wide=false}:{title:string;children:ReactNode;onClose:()=>void;wide?:boolean}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const d=ref.current;d?.showModal();const cancel=(e:Event)=>{e.preventDefault();onClose()};d?.addEventListener('cancel',cancel);return()=>d?.removeEventListener('cancel',cancel)},[onClose]);return <dialog ref={ref} className={'modal '+(wide?'wide':'')}><header><h2>{title}</h2><button className="icon-button" onClick={onClose} aria-label="Đóng"><X size={22}/></button></header>{children}</dialog>}
export function Empty({title,description,action,onAction}:{title:string;description?:string;action?:string;onAction?:()=>void}){return <div className="empty"><div className="empty-symbol"><Plus/></div><h3>{title}</h3>{description&&<p>{description}</p>}{action&&<button className="primary" onClick={onAction}>{action}<ArrowUpRight size={18}/></button>}</div>}
export function Field({label,children,full=false}:{label:string;children:ReactNode;full?:boolean}){return <label className={'field '+(full?'span-2':'')}><span>{label}</span>{children}</label>}
export function Confirm({title,description,onConfirm,onClose}:{title:string;description:string;onConfirm:()=>Promise<void>;onClose:()=>void}){return <Modal title={title} onClose={onClose}><p>{description}</p><div className="form-actions"><button className="secondary" onClick={onClose}>Hủy</button><button className="primary" onClick={()=>onConfirm().then(onClose).catch(()=>{})}>Xác nhận</button></div></Modal>}
