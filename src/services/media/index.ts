export interface MediaStorageProvider {upload(file:File):Promise<{id:string;url:string;order:number;createdAt:string}>;remove(id:string):Promise<void>}
export const mediaStorage:MediaStorageProvider={async upload(file){if(!file.type.startsWith('image/')||file.size>5*1024*1024)throw Error('Chọn ảnh tối đa 5 MB');const url=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=()=>reject(Error('Không đọc được ảnh'));r.readAsDataURL(file)});return {id:crypto.randomUUID(),url,order:0,createdAt:new Date().toISOString()}},async remove(){}};
